import 'package:flutter/material.dart';
class FeedingRoom extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text('수유실 위치'),),
      body: Container(child: Text('fnffn'),),
    );
  }
}