import 'package:flutter/material.dart';
class FindHospital extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text('병원 위치'),),
      body: Container(child: Text('fnffn'),),
    );
  }
}